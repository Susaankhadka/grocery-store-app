import 'package:flutter/material.dart';

class Seikopage extends StatelessWidget {
  const Seikopage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Seiko pages'), centerTitle: true),
    );
  }
}
